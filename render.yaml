services:
  - type: web
    name: tiyende-agribiz
    runtime: static
    staticPublishPath: .
    buildCommand: echo "Static site ready"
    headers:
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
